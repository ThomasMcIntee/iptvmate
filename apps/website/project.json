{
  "name": "website",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/website/src",
  "tags": ["type:website"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/website"],
      "inputs": ["production", "^production"],
      "options": {
        "command": "npx astro build --root apps/website"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx astro dev --root apps/website --port 4321"
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "npx astro preview --root apps/website --port 4322"
      }
    }
  }
}
